---
title: "Individual reflection log"
author: "Ziqi"
editor: visual
---

## Week 1 — Introduction, CLI & Git basics

-   What is one CLI command or git concept you looked up or experimented with beyond what was shown in class? How did you find it, and what does it do?

    -   Git rebase, I forgot to pull and can not push the local change to remote.

-   Describe a moment this week where something didn't work as expected (installation, terminal, git. How did you diagnose and resolve it — and did you use an LLM for help? If so, was the answer good?

    -   I was stuck in terminal and asked ChatGPT, press **Esc** and type :wq, which worked.

-   Why do you think version control is described as "intentional friction"? Do you agree? Write 2–3 sentences from your own perspective.

    -   I do not think so, I feel it is important for parallel collaboration, substituting unrecoverable mistakes with manageable, traceable steps and backup.

## Week 2 — Scripts, Functions & Refactoring

-   Describe a function you wrote or refactored this week. What did you change, and what principle guided that decision?

    -   calculate_delay_rates \<- function(data); reusability and reducing repetition.

-   Did you ask an LLM to help write or improve a function? Paste the prompt you used (or reconstruct it). Did you accept the suggestion as-is? If you changed something, what and why?

    -   I asked Claude to refine a repeated rate calculation into a function. Prompt: "can you refine this part: \[pasted code chunk\] and turn it into a function?" I didn't fully replace my original code but added the refactored version below it, because I need my original code block for later use.

-   What is one R function from a package you discovered this week (in lecture, practical, or on your own) that you hadn't used before? What does it do and when might it be useful?

    -   `cli` package, it lets us write multi-line error messages with argument highlighting.

## Week 3 — Debugging

-   Did you use an LLM to help debug? What did you share with it, and what approach did it take? Was the answer correct? How did you verify it?

    -   I read trackback, copied the console error message and fed into the prompt. I didn't get lots of error but I would chose regression test to verify the code logic.

-   What debugging habit or strategy do you want to deliberately practise going forward? Why that one?

    -   use print statement

    ## Week 4 — Version Control & Collaborative Coding

-   Describe a confusing or frustrating moment with git this week. How did you resolve it? What do you now understand that you didn't before?

    -   git fetch vs. git pull = git fetch + git merge/git rebase

-   How does committing code frequently change how you think about your work? Have you noticed yourself structuring tasks differently because of it?

    -   Committing code frequently helps me break large chunks into smaller, manageable steps and keeps work organized. It also structures tasks so that each change is complete, testable, and easy to review or revert.

## Week 5 — Quarto Websites & Collaborative Coding

-   What is the difference between a Quarto document and a Quarto website project? Describe in your own words what `_quarto.yml` does and how it changes how you think about organising a multi-file project.

    -   A single Quarto document defines all Quarto options in its header; Quarto project is a folder that contains the file `_quarto.yml`.The file `_quarto.yml` provides project configuration, instead of managing each file separately, the shared settings keep related documents consistent.

-   Walk through one `format:` or `execute:` option you set in `_quarto.yml` this week. Why did you choose that option, and what effect did it have on the rendered output?

    -   `format: html` option, toc includes an automatically generated table of contents.

-   Describe a situation this week where you used a git branch, stash, or pull request. What problem were you solving, and would you approach it the same way again?

    -   Git stash, as it temporarily stores unfinished changes to my working copy.

## Week 6 — R Packages

-   Can you think of packages you have used that are very large or very small? Why do you think they contain many or few functions?

    -   tidyverse; it contains several packages

-   How do the ideas of outside-in and inside-out function design relate to package design?

    -   Outside-in starts with what the user actually needs to call; Inside-out starts with domain model, core functions and data structures.

-   `devtools::load_all()` reloads your package without restarting R, whereas `source()`-ing a file just runs it in the global environment. Did you notice a difference in behaviour when using `load_all()`? What does it tell you about how packages work differently from plain scripts?

    -   `dev_tools::load_all` simulates installing and reloading your package, very fast.

-   Writing roxygen2 documentation (`@param`, `@returns`, `@examples`) forces you to describe your function from a user's perspective before you've finished writing it. Did documenting a function change how you thought about its interface or arguments?

    -   Before learning roxygen2 documentation, I mainly focused on whether the code function work. Documenting functions helped me think about how others would understand and reuse them, not just how the code works.

## Week 7 — Initial Data Analysis & Data Cleaning

-   What data format or file type did you encounter that was unfamiliar? How did you read it into R, and what did you learn in the process?

    -   Mainly worked with csv files, load data with `read_csv().`

-   Did you ask an LLM about data licensing, provenance, or quality? What question did you ask, and how did you assess whether the answer was trustworthy?

    -   Yes, I used Claude to check the data quality, which is somehow satisfied.

-   Describe one visualisation you made during IDA. What did you learn from it that a summary statistic alone wouldn't have revealed?

    -   Histogram, it visualized the distribution shape straightforward.

-   What question did your data raise that you hadn't anticipated at the start? Do you think it's worth pursuing, and why?

    -   glimpse data to check the data structure; It helps understanding the dataset, confirming that the data are organized as expected.

## Week 9 — Reproducibility, Open Data & renv

-   Have you set up renv in a project (course or personal)? Describe one dependency or reproducibility issue it helped you avoid or document — or one challenge you encountered when getting it to work.

    -   Different R versions, packages versions, operating system between OS and Windows

-   Did you use a dataset with an explicit open licence (e.g. CC-BY, ODbL, PDDL)? What did the licence allow or restrict, and did that affect how you plan to share or cite it in your final project?

    -   public licence, which allows download, modified, and shared.

-   Describe one documentation gap you discovered in your own or a team member's analysis — something a future reader could not have reproduced or verified without asking you. How did you (or would you) fill it?

    -   Nope so far.

## Week 10 — Analysis & Inference

-   Describe an analysis decision you made — model choice, variable inclusion, aggregation level, etc. What alternatives did you consider, and why did you choose what you did?

    -   I chose quantile regression rather the average effect because the dataset was highly skewed. And I considered it is more robust to extreme values and better suited for capturing relations.

-   If you used an LLM to help reason through a statistical question, describe the exchange. Did the LLM's reasoning hold up? Where did it help, and where did you need to apply your own judgement?

    -   I didn't use LLM to reasoning the statistical question, though it generates ideas but I apply my own judgement to decide which approach is appropriate for the analysis.

    ## Week 11 — Statistical Communication & Visualisation

-   What visualisation principle or technique did you apply this week that you hadn't used before? Where did you learn it (lecture, reading, LLM, experimentation)?

    -   gt table; lecture

-   Describe a figure you revised or improved. What changed between the first and final version, and what drove each change?

    -   a clean theme with theme_minimal()

-   Think of a table or plot you made (or encountered) this week. Would the other format have communicated the same information better or worse — and why? If you used {gt}, what was one formatting choice you made deliberately (column labels, decimal places, spanners, source notes) and what guided it?

    -   In the project, I mainly used plots to analyze patterns, trends, and distributions. I haven't used tables or {gt}, but I would prefer tables to summarize count frequency.
